const express = require('express');
const router = express.Router();

// Define tus rutas para las operaciones CRUD de usuarios.
// router.post('/', ...);
// router.get('/:id', ...);
// router.put('/:id', ...);
// router.delete('/:id', ...);

module.exports = router;
